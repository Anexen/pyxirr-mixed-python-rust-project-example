[tool.poetry]
name = "pyxirr"
version = "0.1.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.6"

xirr = "^0.1.3"


[tool.poetry.dev-dependencies]
pytest = "^5.4.3"
pytest-benchmark = "^3.2.3"
[build-system]
requires = ["maturin"]
build-backend = "maturin"
